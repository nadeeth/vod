'use strict';

describe('myApp.history module', function() {

  beforeEach(module('myApp.history'));

  describe('history controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var historyCtrl = $controller('HistoryCtrl');
      expect(historyCtrl).toBeDefined();
    }));

  });
});